# JChat 域词汇表

> 本文件是词汇表，不含实现细节。

## 搭子 / 伙伴

本应用的桌面虚拟伙伴本体（禁用"桌宠"称呼）。拥有昵称、图标、persona（人格）、移动与主动搭话行为。用户通过聊天窗与它交互。

## 聊天会话（Session）

一次从打开聊天窗到关闭（或跨窗口的连续多轮）的交互单元。会话有滚动窗口（逐字上下文）与会话结束后的记忆抽取。

## 滚动窗口（Window）

最近 N 轮（默认 20 轮）的逐字对话，作为短期上下文喂给 LLM。窗口内的内容随滚动丢弃，不持久化。

## 记忆卡（Memory Card）

每轮注入 system prompt 的蒸馏记忆快照（≤800 token），由长期记忆检索而来。它使 LLM 能"记得"窗口之外与跨会话的事实。组成 = 记忆条目 + 相关三元组；用户记忆优先（预设 6:4，可调）。它不是原始对话，而是事实条目。

## 记忆（Memory）

关于**用户**的事实条目的长期存储（如"用户偏好向量数据库"），存于 memories 表，分短期（short）/长期（long）两层，带重要性、访问计数与衰减分数。会随时间衰减。

## 记忆条目（Memory Item）

"记忆"的单元：单条关于用户的事实，如"用户做过 RAG 项目"。可软关联图谱实体（有则增强检索，缺省不影响写入）。

## 知识库（Knowledge Base）

**世界知识**（非用户事实）的实体-关系图谱，如"LangChain —used_in→ RAG"。来源包括文档（md/txt/pdf/html）与对话抽取；知识不衰减，与记忆分离：记忆是用户事实（衰减），知识是通用知识（持久）。

## 知识三元组（Triple）

知识库的单元：`A —关系→ B`，存于 relations 表，带来源标记（文档或会话）。

## 图谱（Graph）

知识库的网络表示：实体节点 + 关系边。

## 工具（Tool）

搭子通过 LLM function calling 调用的能力：run_python、read_file、write_file、list_files、web_fetch、remember。工具调用在 UI 中以折叠卡片展示。

## 主动搭话

搭子在空闲时随机发起的对话，内容由 LLM 基于 persona 与记忆卡生成（区别于模板句）。

## 人格（Persona）

配置化的说话风格/性格描述，作为 system prompt 的基础部分。

## 语音（Voice）

**语音轮（Voice Turn）**：
一次"按键说话→识别→回复→发声"的完整语音交互循环。

**听觉输入（ASR）**：
把用户说出的话转成文字的环节。_Avoid_: STT、转录、语音识别（作名词时）

**发声（TTS）**：
把搭子的回复文字转成语音播放的环节。_Avoid_: 配音、朗读

**参考音频（Reference Audio）**：
用户提供的音色样本文件；克隆引擎据此生成同音色的新语音。_Avoid_: 音色样本、示例音频

**零样本克隆（Zero-shot Cloning）**：
不训练、仅凭参考音频即时以同音色发声的克隆方式。

**兜底音色（Fallback Voice）**：
克隆引擎不可用时使用的标准预制音色（当前为晓伊）。

**打断（Barge-in）**：
发声期间收到新的用户输入时，立即停止当前播放。
